import { StyledHeader } from "./styles";
import { ReactComponent as Logo } from "../../assets/logo.svg";

export const Header = () => {
  return (
    <StyledHeader>
      <Logo />
    </StyledHeader>
  );
};
